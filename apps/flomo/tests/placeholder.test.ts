import { describe, it, expect } from 'vitest';

/**
 * Placeholder test file for MVP stage.
 * According to CLAUDE.md, MVP stage allows tests to be empty,
 * but we need at least one test file for the test runner to pass.
 *
 * TODO: Add meaningful tests when implementing new features.
 */
describe('Placeholder', () => {
  it('should pass basic sanity check', () => {
    expect(true).toBe(true);
  });

  it('should perform basic arithmetic', () => {
    expect(1 + 1).toBe(2);
  });
});
